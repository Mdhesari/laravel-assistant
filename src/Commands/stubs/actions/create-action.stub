<?php

namespace $NAMESPACE$;

use $MODEL_CLASS$;
use $EVENT_CLASS$;

class $CLASS$
{
    public function __invoke(array $data)
    {
        $model = $MODEL$::create($data);

        event(new $EVENT$($model));

        return $model;
    }
}
