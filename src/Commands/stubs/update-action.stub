<?php

namespace $NAMESPACE$;

use App\Models\$MODEL$;
use App\Events\$EVENT$;

class $CLASS$
{
    public function __invoke(array $data)
    {
        $MODEL$::update($data);

        event(new $EVENT$($model));

        return $model;
    }
}
